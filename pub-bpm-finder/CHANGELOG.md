## 1.0.0

- Initial release of official `bpm_finder_app` package for Dart and Flutter.
- Added `BpmFinder.calculateBpm()` for tap tempo calculations.
- Added `BpmFinder.calculateDelayMs()` for audio delay note division calculations.
